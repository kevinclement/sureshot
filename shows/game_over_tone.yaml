#show_version=5
[
  { duration: 180ms, coils: { cac_chime_high:  { action: pulse } } },
  { duration: 180ms, coils: { cac_chime_high:  { action: pulse } } },
  { duration: 180ms, coils: { cac_chime_high:  { action: pulse } } },
  { duration: 250ms, coils: { cac_chime_mid:   { action: pulse } } },
  { duration: 200ms, coils: { cac_chime_high:  { action: pulse } } },
  { duration: 180ms, coils: { cac_chime_mid:   { action: pulse } } },
]

  # Almost trumpet opening
  # { duration: 150ms, coils: { cac_chime_low:  { action: pulse } } },
  # { duration: 150ms, coils: { cac_chime_mid:  { action: pulse } } },
  # { duration: 150ms, coils: { cac_chime_high: { action: pulse } } },
  # { duration: 150ms, coils: { cac_chime_high:  { action: pulse } } },
  # { duration: 250ms, coils: { cac_chime_high:  { action: pulse } } },
  # { duration: 200ms, coils: { cac_chime_mid: { action: pulse } } },
  # { duration: 200ms, coils: { cac_chime_mid: { action: pulse } } },
  # { duration: 200ms, coils: { cac_chime_mid: { action: pulse } } },
  # { duration: 150ms, coils: { cac_chime_low:  { action: pulse } } },
  # { duration: 250ms, coils: { cac_chime_mid:  { action: pulse } } },
  # { duration: 250ms, coils: { cac_chime_mid:  { action: pulse } } },
  # { duration: 200ms, coils: { cac_chime_high: { action: pulse } } },
  # { duration: 200ms, coils: { cac_chime_high:  { action: pulse } } },